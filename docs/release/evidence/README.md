# Release evidence

Create one immutable directory per gate or release candidate. Store only redacted manifests, reports, checksums and workflow references—never secrets, tenant content, signed URLs or provider payloads.

Current local checkpoints:

- `r0-local-2026-08-29/` — R0 engineering baseline.
- `r1-local-2026-08-29/` — R1 tenant persistence, regulatory and AI-provenance database proof.
